import { atom } from 'jotai';

export const isAudioIncludedAtom = atom(false);

export const listCategoryAtom = atom('');

export const selectedLanguageAtom = atom('');

export const numberOfResultsAtom = atom(10);
